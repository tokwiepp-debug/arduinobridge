name: Build Windows Executable

on:
  workflow_dispatch:
    inputs:
      project_path:
        description: 'Path to project'
        required: false
        default: ''
      python_version:
        description: 'Python version'
        required: false
        default: '3.11'
      artifact_name:
        description: 'Output name'
        required: false
        default: ''

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          path: project

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ github.event.inputs.python_version || '3.11' }}

      - name: Install Nuitka and dependencies
        run: |
          pip install --upgrade pip
          pip install nuitka
          pip install pyserial pyusb requests flask flask-cors

      - name: Build with Nuitka
        shell: pwsh
        run: |
          $path = "project/${{ github.event.inputs.project_path }}"
          $outName = if ("${{ github.event.inputs.artifact_name }}" -ne "") { "${{ github.event.inputs.artifact_name }}" } else { "arduino-bridge" }
          Set-Location $path
          python -m nuitka --onefile --windows-console-mode=attach --output-filename=dist/$outName.exe --remove-output arduino_bridge/server.py 2>&1
          if (Test-Path "dist/$outName.exe") {
            echo "SUCCESS"
            Get-Item "dist/$outName.exe" | Select-Object Name, Length
          } else {
            echo "FAIL - checking for build output..."
            Get-ChildItem . -Recurse -Filter "*.exe" | Select-Object FullName
          }

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.artifact_name || 'arduino-bridge' }}
          path: project/${{ github.event.inputs.project_path }}/dist/
          retention-days: 30
